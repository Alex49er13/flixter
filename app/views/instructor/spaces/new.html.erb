<br />
<div class="booyah-box col-10 offset-1">
  <h1>Add a new section</h1>
  <%= simple_form_for @space, url: instructor_course_spaces_path(@course) do |f| %>
    <%= f.input :title %>
    <br/>
    <%= f.submit "Add a section", class: 'btn btn-primary' %>
    <br />
    <br />
  <% end %>
</div>